<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.adrorodri.programacion3ejemplosmayo2021">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Programacion3EjemplosMayo2021">
        <activity android:name=".CarritoActivity"></activity>
        <activity android:name=".TiendaActivity" />
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".LlenarDatosActivity" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/LoginTheme" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".ThirdActivity" />
    </application>

</manifest>